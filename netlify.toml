[build]
  functions = "netlify/functions"
  publish = "."
  ignore = "git diff --quiet $CACHED_COMMIT HEAD -- . ':!events.json'"

[functions]
  node_bundler = "esbuild"
  included_files = []

[[plugins]]
  package = "@netlify/plugin-functions-install-core"
